# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/legal_scraper?schema=public"

# Redis (BullMQ)
REDIS_URL="redis://localhost:6379"

# Backend
PORT=3001

# Scrape worker (optional)
SCRAPE_WORKER_CONCURRENCY=1

# Frontend dev proxy target (optional; default in vite.config is localhost:3001)
# VITE_API_URL=http://localhost:3001
